<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="android:windowBackground">@color/colorPrimary</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!-- 로그인 화면 -->
    <style name="AppTheme_Login" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/colorPrimary_login</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark_login</item>
        <item name="android:windowBackground">@color/windowBackground_login</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!-- 요금입력화면 EditText 색상-->
    <style name="EditTextTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorControlNormal">@color/bgEditColor</item>
        <item name="colorControlActivated">@color/bgEditColor</item>
        <item name="colorControlHighlight">@color/bgEditColor</item>
    </style>

    <!-- 배차수락 화면 -->
    <style name="AllocSelectTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:statusBarColor">#c9c9c9</item>
    </style>

    <!-- 생년월일 선택 팝업 -->
    <style name="MyDatePicker" parent="android:Widget.DatePicker">
        <item name="android:datePickerMode">spinner</item>
        <item name="android:calendarViewShown">false</item>
    </style>

</resources>
